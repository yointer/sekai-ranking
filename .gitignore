__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# Output MP4s are deliverables — keep them tracked. Override per-project with
# a .gitignore inside the project folder if a particular video shouldn't ship.
